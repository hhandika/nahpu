---
title: "Transfer user configurations"
sidebar:
  order: 4
---

Move controlled vocabularies, export presets, templates, and print layouts without replacing project data.

## Steps

{% steps %}

1. On the source installation, open the project drawer and select `Export user configs`.
2. Choose the configuration sections that the receiving installation needs.
3. Save or share the exported configuration file.
4. On the receiving installation, select `Import user configs` and choose the file.
5. Review replacement warnings, import the selected sections, and verify controlled vocabularies and presets.
6. For a single export preset, document template, or print layout, export just that item from its row, or use `Show QR` on the source and `Scan QR` on the receiving device.
7. If an imported template uses a font this installation does not have, choose a replacement when prompted. Fonts and template images are not part of a transfer, so install the same fonts and add the images when an exact match matters.
{% /steps %}

{% aside type="tip" %}
If a project reaches you without its configuration file, open the vocabulary in Settings and choose `Match database`. NAHPU rebuilds the list from the distinct values already stored in that project's records.
{% /aside %}

Generate a sample table or document on the receiving device to verify transferred presets before production use.

## Learn more

- [Export](https://nahpu.app/en/usages/export/#export-user-configs)
- [Prepare a device and NAHPU](https://nahpu.app/en/how-to-recipes/prepare/prepare-device-and-app/)
- [Settings](https://nahpu.app/en/usages/settings/)
