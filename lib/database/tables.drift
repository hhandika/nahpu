CREATE TABLE project (
    projectUuid TEXT NOT NULL PRIMARY KEY,
    projectName TEXT UNIQUE,
    projectDescription TEXT,
    principalInvestigator TEXT,
    collector TEXT,
    collectorEmail TEXT,
    catNumStart INT,
    catNumEnd INT
);

CREATE TABLE site (
    siteID TEXT NOT NULL PRIMARY KEY,
    projectUuid TEXT,
    leadStuff TEXT,
    type TEXT,
    country TEXT,
    state TEXT,
    preciseLocality TEXT
);

listProject: SELECT projectUuid FROM project;